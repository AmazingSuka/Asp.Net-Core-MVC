@model Product
@{

}

<div class="card card-body bg-light" style="margin-bottom:10px">
    <h3>
        <strong>@Model.Name</strong>
        <span class="badge badge-success float-right">@Model.Price.ToString("c")</span>
    </h3>
    <span class="lead">@Model.Description</span>
</div>